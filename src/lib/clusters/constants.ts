/** Active links required before Clusters tab unlocks. */
export const CLUSTERS_UNLOCK_COUNT = 20;

/** Named clusters must contain at least this many descendant links. */
export const MIN_CLUSTER_SIZE = 5;

/** Soft overview zoom floor for the clusters canvas. */
export const CLUSTERS_MIN_ZOOM = 0.45;

export const GROQ_CLUSTER_MODEL = "llama-3.1-8b-instant";
