/** Soft floor for overview — keeps zoom-out from drowning the GPU. */
export const CLOUD_MIN_ZOOM = 0.42;

/** At this many active links, drop glass blur / motion even near home zoom. */
export const CLOUD_DENSE_COUNT = 48;
